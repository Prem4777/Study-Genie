import React from 'react';

const MemoryMap: React.FC = () => {
  // This component is intentionally left blank to disable the memory map feature
  // as per user request.
  return null;
};

export default MemoryMap;
